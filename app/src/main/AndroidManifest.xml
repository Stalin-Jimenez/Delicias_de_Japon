<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deliciasdejapn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Controller.Registrar_Comida" />
        <activity android:name=".Controller.sushi" />
        <activity android:name=".Controller.dorayaki" />
        <activity android:name=".Controller.teriyaki" />
        <activity android:name=".Controller.ramen" />
        <activity android:name=".Controller.Home" />
        <activity android:name=".Controller.Registrar" />
        <activity android:name=".Controller.Login" />
        <activity android:name=".Controller.PrincipalActivity" />
        <activity
            android:name=".Controller.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>